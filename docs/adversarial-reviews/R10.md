# Revisión adversaria R10

- Fecha UTC: 2026-09-20
- Candidato final: `18446e902fc5ef1ab9151b2247b39fb460aa7375`
- Árbol Git final: `7087a129bc866b1885ccc6ba0953cdc8d83f09a8`
- Revisor: agente adversario `/root/r10_traceable_adversary`
- Alcance: gates acumulativos R0–R10, con foco en backends, I/O,
  incremental, cancelación, TOCTOU y limpieza de recursos

## Historial de revisión

### Primera revisión — PASS

- No se detectaron defectos materiales.
- El revisor no modificó el árbol rastreado.
- El gate autoritativo se verificó en un clon limpio del candidato.

El primer intento del gate sobre el checkout principal rechazó los artefactos
históricos e ignorados bajo `dist/`. No eran parte del candidato. El mismo
commit y árbol completó el gate correctamente en un clon limpio.

## Evidencia adversaria

### Equivalencia de entradas y ejecución

- Bytes, fichero estable, mmap privado, reader/spool e incremental conservan
  evaluación y digest en WideOnce, Deep y DeepVector.
- Los backends siguen recibiendo exclusivamente tareas canónicas.
- El chunking adversario `1, 2, 3, 7, 64, 255, 1024` coincide en las tres
  suites.
- Checkpoints múltiples de las tres suites coinciden con una evaluación directa
  e independiente de cada prefijo.
- Cuatro evaluaciones con ProcessPool dejaron el conjunto de hijos activos en
  `set() → set()`.

### TOCTOU y recursos

- `StableFileSource` rechaza mutaciones entre y durante replays mediante
  `SourceChangedError`.
- Mmap usa una copia temporal privada; truncar o reemplazar el path original no
  altera el mapping evaluado.
- 100 ciclos mmap conservaron los descriptores `/proc/self/fd` en `4 → 4`.
- 200 fallos por límite de spool conservaron los descriptores en `4 → 4`.
- 100 readers cancelados tras I/O conservaron los descriptores en `4 → 4`.
- Se verificaron fichero vacío, límites, cancelación, cierre, facade y módulos
  incluidos en el paquete.

## Validación final

- `python -m scripts.validate_project --fuzz-iterations 200` — PASS.
- `python -m scripts.check_v22_baseline` — PASS.
- `pytest -q tests/unit/test_v3_io.py` — PASS.
- `pytest -q` — PASS.
- `ruff check sigma tests scripts` — PASS.
- `ruff format --check sigma tests scripts` — PASS, 148 archivos.
- Gate autoritativo previo del candidato — 833 tests, 1 skipped;
  `core_formats=96.16026711185309%`, `experimental_scheduler=91.25%` y
  `release_tooling=90.25641025641026%`; fuzz, build, wheel, instalación aislada
  y smokes — PASS.

## Limitaciones

- Es una revisión de ingeniería, no una auditoría criptográfica independiente.
- El fuzzing se limitó a 200 mutaciones por codec.
- Las pruebas de descriptores dependen de `/proc/self/fd`.
- La limpieza de procesos se muestreó con dos workers y cuatro ejecuciones.
- TOCTOU se cubrió con mutaciones y truncados deterministas, no mediante
  instrumentación del kernel.

## Veredicto `PASS`

No queda ningún hallazgo material abierto para R10 en el commit y árbol
finales. Cualquier cambio rastreado en los artefactos R0–R10 invalida este
PASS.
