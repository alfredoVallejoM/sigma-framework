# Revisión adversaria R5

- Fecha UTC: 2026-09-19
- Candidato/commit: `3b079addf92abacf3ff08befedff96adf31a30d7`
- Árbol Git: `4929420c122db5ee2f6ca58da246d97b35082534`
- Revisor: agente adversario `/root/r5_traceable_adversary`
- Alcance: gates acumulativos R0–R5, con foco en layout y placement R5

## Validación previa

El revisor trabajó sobre un worktree limpio y detached en el commit exacto.

- `python -m pytest -q tests/unit/test_v3_layout.py` — 5 passed.
- Pruebas acumulativas R0–R5 — 92 passed.
- `python -m scripts.check_v22_baseline` — PASS.
- `ruff check sigma/layout tests/unit/test_v3_layout.py` — PASS.
- `mypy sigma/layout tests/unit/test_v3_layout.py` — PASS.
- `python -m compileall -q sigma reference scripts tests` — PASS.
- Property check de chunking y slots — 29 009 combinaciones equivalentes al
  helper materializado.
- Exhaustivo reducido de inyectividad — 12 405 entradas distintas sin colisión.
- Distribución sobre 12 000 contextos y 8 slots — todos los estadísticos
  chi-cuadrado quedaron por debajo del umbral crítico de 1 % (`18.475`):
  Anchor `1.749`, Cardinality `7.881`, LengthSignature `5.339` y
  JointSignature `8.871`.

La suite completa alcanzó el 74 % sin fallos antes del límite de observación de
30 segundos; no se utiliza como evidencia del cierre.

## Hallazgos

No se encontraron hallazgos materiales.

| ID | Severidad | Descripción | Resolución |
|---|---|---|---|
| R5-ADV-000 | — | Sin defectos materiales ni incumplimientos del gate | No requerida |

El revisor confirmó domains separados, transcript y tags exactos, cursor SHAKE
único, muestreo inclusivo sin módulo sesgado, slots sobre bytes originales,
orden total de ties, framing inyectivo, recomputación de `LengthSignature`,
límites `uint64`, helper materializado limitado, bordes de chunks y KAT fijo.

## Revalidación y limitaciones

No hubo cambios de código posteriores al candidato ni correcciones que
revalidar. Este informe es el único cambio permitido entre el candidato y el
commit de cierre R5.

Observaciones no bloqueantes y limitaciones declaradas:

- no existe aún una prueba permanente específica de round-trip y malformaciones
  de `LayoutPlan.from_bytes()` y `LayoutPlacement.from_bytes()`; el round-trip
  válido sí se comprobó adversarialmente;
- la comprobación estadística adicional se ejecutó durante la revisión, pero no
  está incorporada como test permanente; el sampler tiene cobertura exhaustiva
  permanente en R4;
- no se ejecutó una matriz física multi-OS/multi-arquitectura; el plan completo
  y el SHA-256 fijo del placement actúan como ancla reproducible;
- no hubo fuzzing exhaustivo del decoder R5;
- no es una auditoría criptográfica externa.

## Veredicto

`PASS`

El PASS sólo es aplicable al commit candidato indicado. Cualquier cambio en los
artefactos R0–R5 obliga a repetir la revisión.
