# Revisión adversaria R9

- Fecha UTC: 2026-09-20
- Candidato final: `5113997424c8b915130695d9f358c322761796fb`
- Árbol Git final: `dc52970b0b22af974ff89a77c67c433d58eb9406`
- Revisor: agente adversario `/root/r9_traceable_adversary`
- Alcance: gates acumulativos R0–R9, con foco en Deep escalar,
  DeepVector, tareas canónicas y equivalencia de backends

## Historial de revisión

### Primera revisión — FAIL

- Commit: `b5a033df656db91ff3cca1846bff4286d2969831`.
- Árbol: `57e35f9ca3130086014efad1507e8125191a9e9b`.
- La semántica R9 y los checks ordinarios pasaban, pero el gate autoritativo
  `python -m scripts.validate_project --fuzz-iterations 200` terminaba con
  `core_formats=93.66%`, por debajo del mínimo inalterado de `95%`.
- El control sobre el padre R8 `8d1916758955a5df0bb5161419ffa46171a41323`
  obtuvo `93.60%`: el déficit era heredado, pero seguía bloqueando el cierre
  acumulativo R9.

### Corrección

El commit `5113997424c8b915130695d9f358c322761796fb` añade pruebas reales de
límites, tipos y rechazos en sólo dos archivos:

- `tests/unit/test_v3_primitives_transcript.py`;
- `tests/unit/test_v3_types.py`.

No modifica código productivo, scripts del gate, configuración, denominadores
ni umbrales. Las pruebas ejercitan ramas productivas de `TranscriptWriter`,
prefijos y records, y secuencias de bytes. Verifican clases y mensajes de error
sin mocks, monkeypatching, pragmas de cobertura ni aserciones vacías.

### Segunda revisión — PASS

- Commit: `5113997424c8b915130695d9f358c322761796fb`.
- Árbol: `dc52970b0b22af974ff89a77c67c433d58eb9406`.
- Verificación realizada en un clon local separado y limpio.
- Todos los hallazgos previos quedaron resueltos y no aparecieron defectos
  semánticos nuevos.

## Evidencia adversaria R9

### Suites y semántica

- `0x0302` permanece reservado al envelope exterior y no es ejecutable.
- Deep usa suite `0x0303`, perfil `0x0302`, estado escalar de 64 bytes y fold
  explícito bajo `DEEP_FOLD (0x0310)`.
- DeepVector usa suite `0x0304`, perfil `0x0303`, conserva `4 × 64` bytes y no
  aplica fold.
- Las tareas capturadas de backend coinciden exactamente con los
  `DeepBranchFrame` canónicos reconstruidos.
- La mutación de un bit en cada una de las 256 posiciones byte del vector
  previo cambió las cuatro ramas sucesoras en todos los casos.
- Serial, threads y procesos produjeron evaluaciones y digests byte-idénticos.
- Un `ProcessPool` correcto de dos workers no dejó procesos hijo vivos.

### KAT y separación de perfiles

Los SHA-256 fijos de los digests R9 coincidieron:

```text
Deep:       274b422d0e361d67f28ebf6dad557357c6f73a125670300cb76fe2d8fc74bc30
DeepVector: 9e84c5065b57928c6c226b682a507b1f5d1456c3329fa288ad7345db4d5ac209
```

No se encontraron defectos en el fold escalar, preservación vectorial,
frames/domains, validación de resultados incorrectos o incompletos, tipos
factory-only ni dispatch de digest y verificación por perfil.

## Gate autoritativo final

Comando:

```text
python -m scripts.validate_project --fuzz-iterations 200
```

Resultado:

- Ruff format y lint — PASS, 214 archivos.
- mypy — PASS, 103 archivos.
- compileall — PASS.
- pytest con cobertura — PASS, 822 tests y 1 skipped.
- `core_formats=96.16026711185309%` — PASS (`>=95%`).
- `experimental_scheduler=91.25%` — PASS (`>=90%`).
- `release_tooling=90.25641025641026%` — PASS (`>=85%`).
- fuzzing — PASS, 200 mutaciones por codec.
- sdist, wheel y metadatos de release — PASS.
- instalación e importación aisladas del wheel — PASS.
- smokes de CLI y runner de experimentos — PASS.
- `python -m scripts.check_v22_baseline` — PASS.
- clon de revisión sin cambios rastreados ni artefactos accidentales.

## Limitaciones

- Es una revisión de ingeniería y conformidad, no una auditoría criptográfica
  externa.
- La dependencia vectorial cubrió una mutación por cada posición byte, no todas
  las combinaciones multibit.
- La limpieza de procesos se muestreó en una ejecución correcta con dos
  workers.
- El fuzzing usó 200 mutaciones por codec y no es exhaustivo.
- Los KAT R9 son fijos y versionados, pero aún no existe una segunda
  implementación independiente R9; ésta corresponde al alcance R12.

## Veredicto `PASS`

No queda ningún hallazgo material abierto para R9 en el commit y árbol finales.
Cualquier cambio rastreado en los artefactos R0–R9 invalida este PASS.
